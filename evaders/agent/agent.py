# evaders/agent — LLM-driven browser agent (headline experiment). STUB.
# Tests whether a reasoning agent defeats the behavioral layer that scripted bots trip.

"""Agent evader — STUB.

Will drive an LLM browser agent (browser-use or Claude Computer Use) through the edge to probe the
behavioral layer — the durable signal that scripted bots cannot fake. Not yet wired — see
architecture §8 and catalog §8.
"""

from __future__ import annotations

NAME = "agent"
VERSION = "0.0.0-stub"
